
import TreeItem from './TreeItem';

function Tree() {
    const children = [].map(x => <Tree>child</Tree>);

    return (<div>
        <TreeItem></TreeItem>
        <ul>
            {children}
        </ul>
        </div>)
}

export default Tree;
